---
file: 17/17-03-atomicity-failsafe.md
block: 17
part: "03"
title: "Атомарность и гонки + Fail-safe (сводка)"
status: закрыт (v3.4)
doc_version: "consolidated v3 + патч v3.4"
contains: [атомарность и гонки, сводка Fail-safe Блока 17]
---

# Блок 17 — Часть 03. Атомарность и гонки + Fail-safe (сводка)

> Источник — [../17-author-panel.md](../17-author-panel.md), разделы «Атомарность и гонки»
> и «🛡 Блок 17 — Fail-safe (сводка)».

## Атомарность и гонки

Атомарность на уровне владельца домена: применение проходит через транзакции блока-владельца; Б17 фиксирует только акт вызова и результат. Гонки решаются CAS-ключами владельцев (Б14) и `expected_*`-контрактом мастера (`expected_state`, `expected_lives`, `expected_photo_hash`, `expected_publish_epoch`). Несовпадение — «no-op с диагнозом», не молчаливое исполнение. Устаревшие callback (`schema_version` mismatch, `nonce` истёк) отбиваются на маршрутизаторе как no-op.

## 🛡 Блок 17 — Fail-safe (сводка)

Вне whitelist — игнор + аудит; при промахе кэша — hard-fail «запретить», при полном отказе БД — bootstrap-режим. Двухшаг обязателен для всего необратимого; кнопка «Да» не на месте первой кнопки. Идемпотентность через стабильный `operation_key`; повтор возвращает оригинальный результат. Callback несёт `schema_version` + `nonce`; устаревшие/чужие — no-op. Тайм-аут мастера 10 минут; один активный мастер на `{actor, cmd, participant}`. `expected_*`-контракт: несовпадение состояния — отказ с диагнозом, нетихое исполнение. Аудит операционный — в одной транзакции с состоянием. `/place` — hard-reject маршрутизатора, включая legacy-callbacks. Кнопки не доступной роли — не рендерятся вовсе (нельзя тапнуть). Автомат не может забанить — маршрутизатор отсекает попытку. Пустых экранов нет: любой отказ выдаёт человекочитаемый текст с причиной.

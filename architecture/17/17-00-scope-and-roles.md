---
file: 17/17-00-scope-and-roles.md
block: 17
part: "00"
title: "Назначение и границы + Модель ролей"
status: закрыт (v3.4)
doc_version: "consolidated v3 + патч v3.4"
contains: [назначение блока, границы, модель ролей (окончательная)]
---

# Блок 17 — Часть 00. Назначение и границы + Модель ролей

> Источник — [../17-author-panel.md](../17-author-panel.md), разделы «Назначение и границы»
> и «Модель ролей (окончательная)». Патч Б17 — [../normative/B17-admin-panel-patch.md](../normative/B17-admin-panel-patch.md).

## Назначение и границы

Блок 17 — исполнительный слой «доступа Бога»: он не вводит бизнес-правил, а даёт Автору и его помощникам безопасные, единообразные, аудируемые входы к правилам блоков-владельцев (Б2 — жизни и паузы, Б6 — карточка, Б7 — рефлексии и «График», Б8 — рабочая группа, Б9 — уведомления, Б10 — жизненный цикл, Б13 — доступ к пройденному, Б14 — durability, Б15 — хранение, Б16 — оплата, юридический блок — `/legal`). Б17 владеет: каталогом команд, командными кнопками-ярлыками, мастером параметров, whitelist-гейтом, моделью ролей, аудитом вызовов, идемпотентностью-обёрткой, UX-контрактом «кнопка → диалог → двухшаг». Б17 не делает: не вводит бизнес-правил, не хранит `/place` и не воскрешает миграцию (поправка v3.1 навсегда), не имеет автоприменения решений, не пишет напрямую в состояние доменов в обход владельцев.

## Модель ролей (окончательная)

Модель — 4 роли. `partner` устранён; технический партнёр присутствует как второй `owner` в whitelist. `owner` — полный доступ: whitelist, финансы, юридический слой, стирания. `moderator` — жизни, паузы, зачёт рефлексий, mute; не видит финансы/юрданные, не банит. `finance` — приём/отказ платежей, refund; без модерации и юрдоступа. `helper` (только чтение состояний и разбор конфликтов) — оставлен явно, тексты корпуса не переписаны, у Б17 нет команд, эксклюзивно закреплённых за `helper`. Whitelist един (глобальный набор ID) и первичен: любое действие вне whitelist — ignore + аудит; кнопки чужой роли — не рендерятся. `helper` не имеет собственных полномочий в командах — если такое понадобится, роль либо получает записи в матрицах, либо обосновывается её сохранение как чтения-only и это фиксируется в тексте.

Команда `/place` и кнопка 📍 Place исключены навсегда поправкой v3.1: маршрутизатор жёстко отбивает legacy-callbacks с префиксом `place_*` по `schema_version`, попытки логируются как `place_attempted` + напоминание в рабочей группе; **NR-17.5** — `/place` не восстанавливается ни при каких условиях.

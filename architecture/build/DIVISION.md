---
file: build/DIVISION.md
block: "—"
title: "Разделение реализации: Mini App / Bot / Backend"
status: канон реализации (Mini App-first)
doc_version: "consolidated v3 + нормативный стек + Mini App-first"
policy: "Проект реализуется как Mini App-first. Основной пользовательский путь — в Mini App. Бот держит только то, что нельзя перенести (уведомления Telegram, admin-команды владельца, доставка ссылки на Mini App, callback-кнопки под сообщениями Автора)."
---

# Разделение реализации: Mini App / Bot / Backend

Три «уровня» реализации — каждый берёт свою часть каждого архитектурного блока. При расхождении с корпусом побеждает корпус; этот файл — карта распределения ответственности.

## Принцип разделения

**Mini App** (клиент, `apps/miniapp/`):
- Все пользовательские экраны (онбординг, дневной модуль, Чек-Ап, Карточка, «Мои согласия», просмотр Карты изменений, оплата, отзыв согласий, экспорт данных).
- Первый запуск = точка создания `pid` (SEAM-PATCH-1).
- Работа с фото 4-х сторон, отправка отчётов, рефлексий.
- CSP `default-src 'self'` + белый список (E2 ERRATA).

**Bot** (Telegram-бот, `apps/bot/`):
- Меню-кнопка → открытие Mini App (`WebAppInfo`, `/setmenubutton`).
- Три-тир уведомления (P-5): системные / напоминания / мотивационные.
- Admin-команды из whitelist (35 команд Б17). **Ровно этот набор — и ничего сверх.**
- Reply-команды в Рабочей группе (`/revert`, `/approve`, `/refund`, `/life`, `/pause_grant`, `/audit`, `/legal`, `/graph`, `/announce`, `/panic_maintenance`).
- Callback-кнопки под сообщениями Бота (командные кнопки Карточки из Б17).
- Whitelist admin-ID + hard-confirm (E3 ERRATA).
- Доставка результата экспорта данных (файл в чат) и уведомлений о завершении стирания.

**Backend** (общий сервер, `apps/api/`):
- OpenAPI-сервер Mini App (`/miniapp/v1/...`).
- Обработчик Telegram Webhook (входящие апдейты для бота).
- Все саги (Refund, Photo Ingest, Red Zone, Erasure).
- `participant_state_projector`, `outbox`, `DLQ`, `export-worker`.
- Единый источник `text_registry`, `role_capability_matrix`, `hard_confirm_phrases`, `topic_bindings`.

## Разделение по блокам

| Блок | Mini App | Bot | Backend |
|---|---|---|---|
| **0. Глоссарий** | — | — | — |
| **1. Общая архитектура** | — | — | Инфра, БД, роли, feature flags (И1). |
| **2. Доступ, оплата, жизни, Пауза** | Экран «Мои этапы», индикатор жизней в шапке, экран «Пауза» (заявка на паузу). | Уведомления о списании жизни, о конце паузы; reply-команды `/life +N`, `/pause_grant`. | FSM участника, атомарность life-ops, три семантики паузы (И2). |
| **3. Механика дня** | Экран дня, чек-лист заданий, кнопка «Отправить отчёт», рефлексия. | Утренний старт-пуш; напоминания «пришёл-не-отправил». | Правила зачёта дня, П-31, гонки по серверному времени. |
| **4. Онбординг** | **ГЛАВНЫЙ ЭКРАН SEAM-1.** First-launch = гейт возраста + `pid` + оплата + анкета + Чек-Ап + правила. | Fallback-заглушка «Открыть в Mini App» при обращении вне Mini App. | Возрастной гейт (`INV-AGE-GATE-BEFORE-PID`, ADD3), запись `pid`. |
| **5. Чек-Ап** | Опросник (5 зон), просмотр результата, кнопка апелляции. | Уведомление о новой публикации Чек-Апа. | Автоскоринг, версионирование конфига, гейт Legal, Red Flags 60-сек эскалация (И3). |
| **6. Карточка участника** | **Участник** — «Мои данные», «Мои согласия», «Моя Карта изменений». **Автор** — Карточка в теме `cards` (в чате бота, не в Mini App). | Открытие Карточки владельцем через `/card` + reply/параметры; callback-кнопки Карточки (Б17). | Динамическая генерация из БД, аудит-лог 152-ФЗ (`pii_access_log`). |
| **7. Контрольные дни и модерация** | Отчёт участника, кнопка «Отправить». | Уведомление «Красный флаг», reply-команда `/revert`, кнопка Approve/Revert под рефлексией. | Р477 (нет `banned_*`), FSM, `/graph` k≥5, теневая пауза (И4). |
| **8. Рабочая группа** | — | Темы TG (`topic_bindings`), зеркало админ-темы, `/panic_maintenance`. | Write-ahead-запись, реактивная сверка (И4). |
| **9. Коммуникации** | Экран «Настройки уведомлений» (чекбоксы: reminders / motivational). | **Отправка всех push через Telegram.** Три-тир политика (P-5). | `text_registry` источник истины, три функции render() по аудитории (И4). |
| **10. Жизненный цикл ([Старт])** | Кнопка «Старт», экран доступности действий по состоянию, экран возвращения после паузы. | «Открыть Mini App» reply-кнопка. | FSM 6 состояний, `participant_state` (E1: обычная таблица с проектором), `publish_epoch` дедуп (FIX1). |
| **11. резерв** | — | — | — |
| **12. резерв (бывш. миграция)** | — | — | — |
| **13. Доступ к пройденному** | Экран «Пройденное», кнопки повтора, revert-контракт без мутации. | — | Триггер `day_completed/13-P`, компенсирующий отзыв из refund saga (И3). |
| **14. Данные, Mini App** | **Клиент Mini App: IndexedDB-кэш, очередь записи, идемпотентность `client_op_id`, тихая переавторизация.** | — | Bootstrapping: outbox, DLQ, контракт бэкапов (И2), CSP E2, S3 в РФ. |
| **15. Хранение/доставка контента + анти-пиратство** | Отправка фото 4 сторон (submit_photo с `client_op_id`), скачивание видео/материалов через pre-signed URL. | Кнопка «Открыть материал» → deep-link в Mini App. | Photo Ingest Saga, export-worker, retention `refund_details` 3 года (И3). Object Lock отключён на фото, WORM на аудите. |
| **16. Оплата** | Экран оплаты (форма PSP), экран статуса возврата, отзыв, акт услуг. | Уведомление о зачислении/отклонении оплаты, reply `/refund`, кнопка «Оформить возврат». | Refund Saga, часовая сверка с PSP, `refund_details` retention (И3). |
| **17. Панель Автора** | **Не переносится в Mini App.** Владелец работает через Рабочую группу + reply-команды. | **Полностью в боте.** 35 команд Б17, hard-confirm E3, whitelist. Дашборд/palette — как отдельные reply-команды с рендером в бот-теме. | `role_capability_matrix`, `admin_action_log`, аудит доступа (`pii_access_log`). |
| **99. Юридический** | Просмотр текстов согласий, «Мои согласия» с отзывом. | Уведомления boot-gate / изменений оферты (после юр.вычитки). | `text_registry.legal_status`, boot-gate, C0–C6, `erasure_blacklist`. |

## Что реализуется в Mini App **приоритетно** (MVP-минимум)

Порядок первых итераций (первые 4–6 недель):

1. **Онбординг + Mini App skeleton** — SEAM-1, возрастной гейт, создание `pid`, оплата первого этапа (Блок 4).
2. **Экран дня + отправка отчёта + жизни** (Блоки 2, 3).
3. **Чек-Ап + Карточка участника (сторона Участника)** (Блоки 5, 6).
4. **Клиент Mini App v3.4** — IndexedDB, идемпотентность, CSP (Блок 14).
5. **Refund Saga через Mini App** (Блок 16).

## Что реализуется в боте **приоритетно**

1. **Menu Button → Mini App URL** (`/setmenubutton` в BotFather).
2. **Whitelist admin-ID + hard-confirm** — Б17, 35 команд.
3. **Reply-команды в Рабочей группе** — `/revert`, `/approve`, `/refund`, `/life`, `/pause_grant`.
4. **Три-тир push** — уведомления через Bot API.
5. **Fallback-заглушка** для участников, открывших бота без Mini App: «Установите последнюю версию Telegram и откройте кнопку меню».

## Что НЕ переносим в Mini App (осознанное решение)

- **Панель Автора (Блок 17)** остаётся в боте. Причина: 35 admin-команд + hard-confirm + двухшаг + reply-parameters — это интерфейс, где Mini App даёт больше рисков (сложнее аудит), чем ценности. Владелец и так живёт в Рабочей группе.
- **Массовая рассылка `/announce` и broadcast-инспектор** — только в боте (Bot API проще; в Mini App были бы дубли).
- **`/panic_maintenance`** — только в боте (должен работать даже при полностью упавшем Mini App).

## Как этот файл читать агенту

При задаче «реализуй блок N» — открывай этот файл первым, находи строку блока и распределяй код по трём подкаталогам:

```
apps/miniapp/   ← фронт Mini App (React + Vite + @telegram-apps/sdk-react)
apps/bot/       ← Telegram-бот (Python + aiogram / grammY)
apps/api/       ← backend (FastAPI + PostgreSQL + Redis)
```

Если задача пришла без указания блока — открой [../CONTEXT.md](../CONTEXT.md), потом [../AGENTS.md](../AGENTS.md), потом этот файл.
